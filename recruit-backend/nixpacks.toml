# Pin Python version for Railway Nixpacks builder.
[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "pip install -e ."
]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2 --timeout-keep-alive 30"
